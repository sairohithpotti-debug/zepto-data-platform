readme_content = """# Module 1: Data Pipeline (/data_pipeline)

## Overview
This module automates an end-to-end data pipeline for Zepto competitive intelligence. It scrapes raw catalog data from books.toscrape.com, cleans and normalizes the fields, converts currency rates using a fixed project baseline, loads the data into a structured relational SQLite database, and executes SQL/Pandas analytics side-by-side.

---

## Installation & Setup

### Prerequisites
* Python 3.8+
* Required Libraries: requests, beautifulsoup4, pandas

### Install Dependencies
```bash
pip install requests beautifulsoup4 pandas"""
